import 'package:flutter/material.dart';

class RemoveScreen extends StatelessWidget {
  const RemoveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Remove - remove items from your fridge or inventory'),
    );
  }
}
